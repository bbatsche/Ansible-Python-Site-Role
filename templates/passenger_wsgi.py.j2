def application(environ, start_response):
    response_body = b"Phusion Passenger is serving Python code on {{ domain }}.\n"

    start_response('200 OK', [('Content-Type',  'text/html')])

    yield response_body
