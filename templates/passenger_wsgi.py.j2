def application(environ, start_response):
    response_body = b"Phusion Passenger is serving Python {{ python_version }} code on {{ domain }}.\n"

    start_response('200 OK', [('Content-Type',  'text/plain')])

    yield response_body
